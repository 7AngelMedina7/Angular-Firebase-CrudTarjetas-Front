import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.example.app',
  appName: 'TarjetasCredito',
  webDir: 'dist/tarjetas-credito',
  bundledWebRuntime: false
};

export default config;
